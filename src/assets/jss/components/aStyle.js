import {
    defaultFont
} from '../theme';

const aStyle = theme => ({
    a: {
        ...defaultFont,
        margin: '0',
        color: 'white',
        outlone: 'none',
        textDecoration: 'none'
    }
});

export default aStyle;