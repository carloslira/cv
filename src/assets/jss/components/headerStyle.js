import {
    primaryLightColor
} from '../theme';

const headerStyle = theme => ({
    container: {
        minHeight: '200px',
        backgroundColor: primaryLightColor
    }
});

export default headerStyle;