import {
    defaultFont
} from '../theme';

const experiencesStyle = theme => ({
    a: {
        ...defaultFont,
        margin: '0',
        color: 'white',
        outline: 'none',
        textDecoration: 'none'
    }
});

export default experiencesStyle;