import {
    lightGrayColor
} from "../theme";

const appStyle = theme => ({
	wrapper: {
		top: '0',
		height: '100vh',
        position: 'relative',
        backgroundColor: lightGrayColor
	}
});

export default appStyle;